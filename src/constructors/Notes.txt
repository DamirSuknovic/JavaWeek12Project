Constructors are special methods, it is a MUST for a class, it might be used to initialize your fields (variables)

Object: new ClassName() ---> we are invoking/calling the constructor

Creating of Constructor:
accessModifiers className(){
    statements
}

When do we need this. keyword?
- When your parameter variable name is same with the instance variable name

Task1:
Employee:
    attributes/fields/variables
name, jobTitle, ID, salary

    add a constructor to initialize all the fields
    add a default constructor with no fields

    toString() method will print out the fields
    - instance method (